# Bitácora de Desarrollo - Nuevos Campos en Postulaciones y Mantenedor de Niveles de Experiencia

**Fecha:** 2026-06-03  
**Hora de Registro:** 00:47:50 (Hora Local)  
**Tema Principal:** Implementación de nuevos campos (Nivel, Cantidad de Solicitudes, Sueldo Pedido), soporte de vista de solo lectura ("ojo" de detalle) y creación del mantenedor CRUD para Niveles de Experiencia.

---

## 1. Introducción y Requerimientos

En esta jornada de desarrollo se implementaron mejoras clave al Gestor de Postulaciones Laborales. El objetivo principal era añadir nuevos atributos a las postulaciones para un mejor seguimiento de los procesos y crear la infraestructura necesaria para clasificar y administrar los niveles de experiencia asociados.

Los requerimientos detallados del usuario consistían en:
1. **Cantidad de solicitudes**: Registrar de manera opcional cuántas postulaciones/solicitudes se han enviado para la oferta (ej. en plataformas como LinkedIn o GetOnBoard).
2. **Nivel de experiencia**: Registrar de manera opcional el nivel de la postulación (Trainee, Junior, Semi Senior, Senior). Se sugirió y aprobó crear una tabla dedicada (`nivel_experiencia`) junto con su mantenedor CRUD correspondiente.
3. **Sueldo pedido**: Campo numérico para guardar el sueldo pretendido o solicitado por el usuario (diferente del sueldo ofrecido por la vacante).
4. **Optimización del Formulario**: Reducir el tamaño visual del campo "Descripción" para dar cabida a los nuevos campos sin saturar el formulario.
5. **Botón de Detalle ("Ojo")**: Añadir un botón con forma de ojo en la lista de postulaciones para abrir la ficha en modo solo lectura (`readOnly`), permitiendo revisar todos los campos pero sin posibilidad de edición.
6. **Mantenedor de Niveles**: Crear una página CRUD para los niveles de experiencia, permitiendo crear, editar, eliminar y reordenar dichos niveles.
7. **Integración en Navegación**: Registrar la nueva vista de niveles en el Sidebar del sistema.

---

## 2. Archivos Afectados e Historial de Cambios

A continuación se detalla la lista de todos los archivos creados, modificados o reestructurados durante el desarrollo de la tarea:

| # | Archivo | Acción | Descripción Corta del Cambio |
|---|---------|--------|------------------------------|
| 1 | [docs/Pasos_Inciales/schema.sql](file:///c:/Users/djara/Documents/REPOSITORIOSyLABORAL/POSTULACIONES/postulaciones_analisis_final/docs/Pasos_Inciales/schema.sql) | `MODIFICAR` | Creación de tabla `nivel_experiencia`, adición de campos en `postulacion` e inserción de datos semilla. |
| 2 | [server/index.ts](file:///c:/Users/djara/Documents/REPOSITORIOSyLABORAL/POSTULACIONES/postulaciones_analisis_final/server/index.ts) | `MODIFICAR` | Whitelist de tablas permitidas, integración del JOIN en consulta GET y serialización a JSON del objeto `nivel_experiencia`. |
| 3 | [src/lib/api.ts](file:///c:/Users/djara/Documents/REPOSITORIOSyLABORAL/POSTULACIONES/postulaciones_analisis_final/src/lib/api.ts) | `MODIFICAR` | Definición de tipo `NivelExperiencia` y adición de campos nuevos a la interfaz `PostulacionRow`. |
| 4 | [src/hooks/useData.ts](file:///c:/Users/djara/Documents/REPOSITORIOSyLABORAL/POSTULACIONES/postulaciones_analisis_final/src/hooks/useData.ts) | `MODIFICAR` | Creación del hook de carga de datos `useNiveles()`. |
| 5 | [src/components/postulaciones/PostulacionForm.tsx](file:///c:/Users/djara/Documents/REPOSITORIOSyLABORAL/POSTULACIONES/postulaciones_analisis_final/src/components/postulaciones/PostulacionForm.tsx) | `MODIFICAR` | Reestructuración de columnas de formulario, reducción de "Descripción", inserción de nuevos campos y soporte completo a la prop `readOnly`. |
| 6 | [src/pages/PostulacionesPage.tsx](file:///c:/Users/djara/Documents/REPOSITORIOSyLABORAL/POSTULACIONES/postulaciones_analisis_final/src/pages/PostulacionesPage.tsx) | `MODIFICAR` | Inclusión del hook `useNiveles()`, renderizado del botón de ojo (vista solo lectura) y paso de parámetros actualizados. |
| 7 | [src/pages/NivelesPage.tsx](file:///c:/Users/djara/Documents/REPOSITORIOSyLABORAL/POSTULACIONES/postulaciones_analisis_final/src/pages/NivelesPage.tsx) | `NUEVO` | Mantenedor CRUD completo e independiente para gestionar la tabla `nivel_experiencia`. |
| 8 | [src/components/layout/Sidebar.tsx](file:///c:/Users/djara/Documents/REPOSITORIOSyLABORAL/POSTULACIONES/postulaciones_analisis_final/src/components/layout/Sidebar.tsx) | `MODIFICAR` | Registro de la ruta de navegación 'niveles' en el panel de configuraciones laterales. |
| 9 | [src/App.tsx](file:///c:/Users/djara/Documents/REPOSITORIOSyLABORAL/POSTULACIONES/postulaciones_analisis_final/src/App.tsx) | `MODIFICAR` | Importación e inclusión del switch-case de renderizado de la página `NivelesPage`. |

---

## 3. Detalle Técnico de la Implementación

### 3.1. Migración de Base de Datos SQL

Para actualizar la estructura del motor PostgreSQL sin comprometer la información existente, se definieron sentencias que no arrojaran errores en caso de re-ejecución.

El script de base de datos se plasmó en el archivo [schema.sql](file:///c:/Users/djara/Documents/REPOSITORIOSyLABORAL/POSTULACIONES/postulaciones_analisis_final/docs/Pasos_Inciales/schema.sql) y se ejecutó en la base de datos local:

```sql
-- 1. Tabla de Niveles de Experiencia
CREATE TABLE IF NOT EXISTS nivel_experiencia (
  id serial PRIMARY KEY,
  nombre text NOT NULL UNIQUE,
  orden integer DEFAULT 0,
  created_at timestamptz DEFAULT now()
);

-- 2. Inserción de Niveles Semilla
INSERT INTO nivel_experiencia (nombre, orden) VALUES
  ('Trainee', 1),
  ('Junior', 2),
  ('Semi Senior', 3),
  ('Senior', 4)
ON CONFLICT (nombre) DO NOTHING;

-- 3. Modificaciones a la Tabla Postulación (ALTER TABLE)
ALTER TABLE postulacion 
  ADD COLUMN IF NOT EXISTS cantidad_solicitudes integer,
  ADD COLUMN IF NOT EXISTS id_nivel integer REFERENCES nivel_experiencia(id) ON DELETE SET NULL,
  ADD COLUMN IF NOT EXISTS sueldo_pedido numeric(12,2);
```

*Nota: La adición de `ON DELETE SET NULL` previene la pérdida de registros de postulaciones si es que algún nivel de experiencia es eliminado del sistema.*

---

### 3.2. Desarrollo en el Backend (API)

En el backend, configurado sobre Express, se incluyó soporte para la nueva tabla y se modificó la consulta principal de búsqueda.

#### Whitelist de Tablas Permitidas:
En el archivo [server/index.ts](file:///c:/Users/djara/Documents/REPOSITORIOSyLABORAL/POSTULACIONES/postulaciones_analisis_final/server/index.ts) se incluyó `'nivel_experiencia'` a la whitelist `ALLOWED_TABLES` para habilitar las operaciones CRUD genéricas:
```typescript
const ALLOWED_TABLES = [
  'empresa', 
  'cargo', 
  'estado', 
  'plataforma', 
  'modalidad', 
  'ubicacion', 
  'tecnologia', 
  'metodo_evaluacion', 
  'nivel_experiencia' // Nuevo
];
```

#### Modificación del Query GET:
En la ruta `GET /api/postulaciones`, se actualizó la consulta para hacer un `LEFT JOIN` con la tabla `nivel_experiencia` y serializarla mediante la función `row_to_json()` de PostgreSQL:
```typescript
// SELECT principal con JSON mapping
const mainQ = `
  SELECT p.*,
    row_to_json(e) as empresa, row_to_json(c) as cargo,
    row_to_json(s) as estado, row_to_json(pl) as plataforma,
    row_to_json(m) as modalidad, row_to_json(u) as ubicacion,
    row_to_json(ne) as nivel_experiencia -- Nuevo JOIN serializado
  FROM postulacion p
  LEFT JOIN empresa e ON p.id_empresa = e.id
  LEFT JOIN cargo c ON p.id_cargo = c.id
  LEFT JOIN estado s ON p.id_estado = s.id
  LEFT JOIN plataforma pl ON p.id_plataforma = pl.id
  LEFT JOIN modalidad m ON p.id_modalidad = m.id
  LEFT JOIN ubicacion u ON p.id_ubicacion = u.id
  LEFT JOIN nivel_experiencia ne ON p.id_nivel = ne.id -- Nuevo JOIN
  ${where}
  ORDER BY ${col} ${dir}
  LIMIT $${idx++} OFFSET $${idx++}
`;
```

---

### 3.3. Configuración en la Capa de Datos del Frontend (Tipos e Hooks)

Para mantener la coherencia del tipado estático en TypeScript, se actualizó la definición de datos de la API.

#### Archivo `src/lib/api.ts`:
```typescript
// Definición del tipo
export type NivelExperiencia = { 
  id: number; 
  nombre: string; 
  orden?: number; 
  created_at?: string 
};

// Nuevos campos agregados a PostulacionRow
export type PostulacionRow = {
  id: number;
  descripcion: string;
  id_empresa: number | null;
  id_cargo: number | null;
  id_estado: number;
  url: string | null;
  id_plataforma: number | null;
  id_modalidad: number | null;
  id_ubicacion: number | null;
  dias_presenciales: number | null;
  sueldo_ofrecido: number | null;
  
  // Nuevos atributos
  cantidad_solicitudes: number | null;
  id_nivel: number | null;
  sueldo_pedido: number | null;
  
  fecha_postulacion: string;
  fecha_actualizacion: string;
  created_at?: string;
  
  empresa?: Empresa;
  cargo?: Cargo;
  estado?: Estado;
  plataforma?: Plataforma;
  modalidad?: Modalidad;
  ubicacion?: Ubicacion;
  
  // Nueva relación
  nivel_experiencia?: NivelExperiencia;
  
  tecnologias?: Tecnologia[];
  metodos?: MetodoEvaluacion[];
};
```

#### Archivo `src/hooks/useData.ts`:
Para realizar llamadas dinámicas a la ruta genérica del backend, se añadió el hook `useNiveles`:
```typescript
export function useNiveles() { 
  return useCatalog<NivelExperiencia>('nivel_experiencia'); 
}
```

---

### 3.4. Formulario de Creación y Edición (`PostulacionForm.tsx`)

El formulario de postulación [PostulacionForm.tsx](file:///c:/Users/djara/Documents/REPOSITORIOSyLABORAL/POSTULACIONES/postulaciones_analisis_final/src/components/postulaciones/PostulacionForm.tsx) requirió las modificaciones más exhaustivas para acomodar los nuevos elementos y gestionar las restricciones del modo de lectura.

#### Actualización del Estado Local y Valores por Defecto:
```typescript
type FormState = {
  id_empresa: string;
  id_cargo: string;
  id_estado: string;
  url: string;
  id_plataforma: string;
  id_modalidad: string;
  id_ubicacion: string;
  dias_presenciales: string;
  sueldo_ofrecido: string;
  cantidad_solicitudes: string; // Nuevo
  id_nivel: string;             // Nuevo
  sueldo_pedido: string;        // Nuevo
  fecha_postulacion: string;
  descripcion: string;
  tecnologias: number[];
  metodos: number[];
};

function toForm(item: PostulacionRow | null, estadoDefault: number): FormState {
  if (!item) {
    const today = new Date().toISOString().split('T')[0];
    return {
      id_empresa: '', id_cargo: '', id_estado: String(estadoDefault),
      url: '', id_plataforma: '', id_modalidad: '', id_ubicacion: '',
      dias_presenciales: '', sueldo_ofrecido: '',
      cantidad_solicitudes: '', id_nivel: '', sueldo_pedido: '', // Nuevos
      fecha_postulacion: today, descripcion: '',
      tecnologias: [], metodos: [],
    };
  }
  return {
    id_empresa: String(item.id_empresa ?? ''),
    id_cargo: String(item.id_cargo ?? ''),
    id_estado: String(item.id_estado),
    url: item.url ?? '',
    id_plataforma: String(item.id_plataforma ?? ''),
    id_modalidad: String(item.id_modalidad ?? ''),
    id_ubicacion: String(item.id_ubicacion ?? ''),
    dias_presenciales: String(item.dias_presenciales ?? ''),
    sueldo_ofrecido: String(item.sueldo_ofrecido ?? ''),
    cantidad_solicitudes: String(item.cantidad_solicitudes ?? ''), // Nuevo
    id_nivel: String(item.id_nivel ?? ''),                         // Nuevo
    sueldo_pedido: String(item.sueldo_pedido ?? ''),               // Nuevo
    fecha_postulacion: item.fecha_postulacion ? item.fecha_postulacion.split('T')[0] : '',
    descripcion: item.descripcion ?? '',
    tecnologias: item.tecnologias?.map(t => t.id) ?? [],
    metodos: item.metodos?.map(m => m.id) ?? [],
  };
}
```

#### Envío de Payload al Servidor (Tipado Numérico):
En la función `handleSave`, los nuevos strings ingresados son parseados a números o manejados como nulos:
```typescript
const body = {
  id_empresa: form.id_empresa ? Number(form.id_empresa) : null,
  id_cargo: form.id_cargo ? Number(form.id_cargo) : null,
  id_estado: Number(form.id_estado),
  url: form.url || null,
  id_plataforma: form.id_plataforma ? Number(form.id_plataforma) : null,
  id_modalidad: form.id_modalidad ? Number(form.id_modalidad) : null,
  id_ubicacion: form.id_ubicacion ? Number(form.id_ubicacion) : null,
  dias_presenciales: form.dias_presenciales ? Number(form.dias_presenciales) : null,
  sueldo_ofrecido: form.sueldo_ofrecido ? Number(form.sueldo_ofrecido) : null,
  
  // Nuevos campos mapeados numéricamente
  cantidad_solicitudes: form.cantidad_solicitudes ? Number(form.cantidad_solicitudes) : null,
  id_nivel: form.id_nivel ? Number(form.id_nivel) : null,
  sueldo_pedido: form.sueldo_pedido ? Number(form.sueldo_pedido) : null,
  
  fecha_postulacion: form.fecha_postulacion,
  descripcion: form.descripcion,
  tecnologias: form.tecnologias,
  metodos: form.metodos,
};
```

#### Adaptación del Componente Reusable `ComboBox`:
Para que las entradas de Empresa y Cargo respetaran el modo solo lectura, se modificaron los tipos e implementaciones de ComboBox:
```typescript
interface ComboBoxProps {
  label: string;
  placeholder: string;
  items: { id: number; nombre: string }[];
  value: string;
  onChange: (id: string) => void;
  onCreateNew: (nombre: string) => Promise<void>;
  entityLabel: string;
  readOnly?: boolean; // Nuevo
}

function ComboBox({ label, placeholder, items, value, onChange, onCreateNew, entityLabel, readOnly = false }: ComboBoxProps) {
  const [text, setText] = useState('');
  const [open, setOpen] = useState(false);
  const [creating, setCreating] = useState(false);
  const ref = useRef<HTMLDivElement>(null);

  // ... lógica interna ...

  return (
    <div ref={ref} className="relative">
      <label className="text-xs text-slate-400 mb-1 block">{label}</label>
      <div className="flex gap-1.5">
        <div className="relative flex-1">
          <input
            type="text"
            value={text}
            onChange={e => handleInputChange(e.target.value)}
            onFocus={() => !readOnly && setOpen(true)} // No abre si es readOnly
            placeholder={placeholder}
            disabled={readOnly} // Deshabilitado en readOnly
            className="w-full bg-slate-700 border border-slate-600 text-slate-100 text-sm rounded-lg px-3 py-2 pr-8 focus:outline-none focus:border-blue-500 disabled:opacity-60"
          />
          <button
            type="button"
            onClick={() => !readOnly && setOpen(!open)}
            disabled={readOnly}
            className="absolute right-2 top-1/2 -translate-y-1/2 text-slate-400 hover:text-slate-200 transition-colors disabled:opacity-60"
          >
            <ChevronDown size={16} className={`transition-transform ${open ? 'rotate-180' : ''}`} />
          </button>
        </div>
        <button
          type="button"
          onClick={handleCreate}
          disabled={!canCreate || creating || readOnly} // Deshabilitado en readOnly
          title={existsAlready && text.trim().length > 0 ? `"${text.trim()}" ya existe` : `Agregar ${entityLabel}`}
          className="flex items-center justify-center w-9 h-9 bg-emerald-600 hover:bg-emerald-500 disabled:bg-slate-600 disabled:text-slate-500 text-white rounded-lg transition-colors flex-shrink-0"
        >
          <Plus size={16} />
        </button>
      </div>
      
      {/* Listado del Combobox */}
      {open && !readOnly && (
        <div className="absolute z-50 mt-1 w-full bg-slate-700 border border-slate-600 rounded-lg shadow-xl max-h-44 overflow-y-auto">
          {/* ... render de items ... */}
        </div>
      )}
    </div>
  );
}
```

#### Maquetación del Formulario en Columnas:
Para evitar la saturación visual, se reordenaron los componentes internos del formulario.
1. **Columna 1**:
   - Empresa (ComboBox con `readOnly`)
   - Cargo (ComboBox con `readOnly`)
   - Nivel (SelectDropdown con `readOnly`)
   - Plataforma (SelectDropdown con `readOnly`)
   - Bloque de dos columnas: Fecha de Postulación + Cantidad de Solicitudes (Input numérico)
   - Notas / Descripción: Textarea reducido a `rows={2}`.
2. **Columna 2**:
   - Tags de tecnologías. En modo lectura, no muestran la "X" para eliminar y no permiten añadir tecnologías nuevas.
   - Lista de tecnologías. Se deshabilitan los clicks de toggle mediante la validación `!readOnly && toggleTecno()`.
   - Métodos de Evaluación. Ídem que tecnologías.
3. **Columna 3**:
   - Ubicación (SelectDropdown con `readOnly`)
   - Modalidad (SelectDropdown con `readOnly`)
   - Días Híbridos (Input numérico con `readOnly`)
   - Sueldo Ofrecido (Input numérico con `readOnly`)
   - Sueldo Pedido (Input numérico con `readOnly`, con styling en verde esmeralda y el texto descriptivo "por ti")
   - Estado (SelectDropdown con `readOnly`)
   - URL de Oferta (Input URL con `readOnly`)

#### Botonera del Formulario:
```typescript
{/* Botonera Condicional */}
{!readOnly ? (
  <div className="flex items-center justify-center gap-3 pt-2 border-t border-slate-700">
    <button
      onClick={handleSave}
      disabled={saving}
      className="px-8 py-2.5 bg-blue-600 hover:bg-blue-500 disabled:opacity-50 text-white text-sm font-bold uppercase tracking-wider rounded-lg transition-colors"
    >
      {saving ? 'Guardando...' : 'Guardar Postulación'}
    </button>
    <button
      onClick={onCancel}
      className="px-6 py-2.5 bg-slate-700 hover:bg-slate-600 text-slate-200 text-sm font-medium rounded-lg transition-colors"
    >
      Cancelar
    </button>
  </div>
) : (
  <div className="flex items-center justify-center pt-2 border-t border-slate-700">
    <button
      onClick={onCancel}
      className="px-8 py-2.5 bg-slate-700 hover:bg-slate-600 text-slate-200 text-sm font-medium rounded-lg transition-colors"
    >
      Cerrar
    </button>
  </div>
)}
```

---

### 3.5. Incorporación de Detalles en la Lista Principal (`PostulacionesPage.tsx`)

Se añadió un estado en `PostulacionesPage` llamado `formReadOnly` para determinar cómo se debe inicializar el diálogo modal.

#### Modificación de las Funciones de Apertura:
```typescript
const [formReadOnly, setFormReadOnly] = useState(false);

const openNew = () => { 
  setFormReadOnly(false); 
  setEditing(null); 
  setFormOpen(true); 
};

const openEdit = (row: PostulacionRow, readOnly: boolean) => { 
  setFormReadOnly(readOnly); 
  setEditing(row); 
  setFormOpen(true); 
};
```

#### Botonera de Acciones de la Fila (Eye Icon):
Se importó `Eye` de `lucide-react` y se agregaron los botones a la tabla:
```typescript
<td className="px-3 py-2.5 text-right">
  <div className="flex items-center justify-end gap-2 opacity-0 group-hover:opacity-100 transition-opacity">
    {/* Botón de solo lectura */}
    <button
      onClick={() => openEdit(row, true)}
      title="Ver Detalle"
      className="p-1.5 text-slate-400 hover:text-emerald-400 hover:bg-slate-700 rounded transition-all"
    >
      <Eye size={14} />
    </button>
    
    {/* Botón de edición */}
    <button
      onClick={() => openEdit(row, false)}
      title="Editar"
      className="p-1.5 text-slate-400 hover:text-blue-400 hover:bg-slate-700 rounded transition-all"
    >
      <Pencil size={14} />
    </button>
    
    {/* Botón de eliminación */}
    <button
      onClick={() => setDeleteId(row.id)}
      title="Eliminar"
      className="p-1.5 text-slate-400 hover:text-red-400 hover:bg-slate-700 rounded transition-all"
    >
      <Trash2 size={14} />
    </button>
  </div>
</td>
```

---

### 3.6. Nuevo Mantenedor CRUD (`NivelesPage.tsx`)

Siguiendo el diseño e infraestructura del archivo `CargosPage.tsx`, se creó un nuevo mantenedor modular. Este mantenedor consume la API mediante el hook `useNiveles()`.

A continuación, el código completo de la página CRUD:

```typescript
import { useState } from 'react';
import { Layers } from 'lucide-react';
import { apiFetch } from '../lib/api';
import type { NivelExperiencia } from '../lib/api';
import { useNiveles } from '../hooks/useData';
import MaintainerTable from '../components/maintainer/MaintainerTable';
import Modal from '../components/ui/Modal';
import Header from '../components/layout/Header';

interface Props { onMenuOpen: () => void; }

export default function NivelesPage({ onMenuOpen }: Props) {
  const { data, loading, reload } = useNiveles();
  const [page, setPage] = useState(1);
  const perPage = 15;
  const [editing, setEditing] = useState<NivelExperiencia | null>(null);
  const [formOpen, setFormOpen] = useState(false);
  const [nombre, setNombre] = useState('');
  const [orden, setOrden] = useState<number>(0);
  const [deleteItem, setDeleteItem] = useState<NivelExperiencia | null>(null);
  const [saving, setSaving] = useState(false);

  const openNew = () => { setEditing(null); setNombre(''); setOrden(0); setFormOpen(true); };
  const openEdit = (row: NivelExperiencia) => { setEditing(row); setNombre(row.nombre); setOrden(row.orden ?? 0); setFormOpen(true); };

  const handleSave = async () => {
    if (!nombre.trim()) return;
    setSaving(true);
    const body = { nombre, orden: Number(orden) };
    try {
      if (editing) {
        await apiFetch(`/nivel_experiencia/${editing.id}`, { method: 'PUT', body: JSON.stringify(body) });
      } else {
        await apiFetch('/nivel_experiencia', { method: 'POST', body: JSON.stringify(body) });
      }
      setFormOpen(false);
      reload();
    } catch (err) {
      console.error(err);
    } finally {
      setSaving(false);
    }
  };

  const handleDelete = async () => {
    if (!deleteItem) return;
    try {
      await apiFetch(`/nivel_experiencia/${deleteItem.id}`, { method: 'DELETE' });
      setDeleteItem(null);
      reload();
    } catch (err) {
      console.error(err);
    }
  };

  const paged = data.slice((page - 1) * perPage, page * perPage);

  return (
    <div className="flex flex-col h-full">
      <Header onMenuOpen={onMenuOpen} search="" onSearch={() => {}} showSearch={false} />
      <MaintainerTable
        title="Mantenedor de Niveles de Experiencia"
        icon={<Layers size={18} className="text-slate-400" />}
        rows={paged}
        columns={[
          { key: 'nombre', label: 'Nombre del Nivel' },
          { key: 'orden', label: 'Orden' }
        ]}
        total={data.length}
        page={page}
        perPage={perPage}
        onPage={setPage}
        onNew={openNew}
        onEdit={openEdit}
        onDelete={setDeleteItem}
        loading={loading}
      />

      <Modal open={formOpen} onClose={() => setFormOpen(false)} title={editing ? 'Editar Nivel' : 'Nuevo Nivel'} size="sm">
        <div className="space-y-4">
          <div>
            <label className="text-xs text-slate-400 mb-1 block">Nombre</label>
            <input type="text" value={nombre} onChange={e => setNombre(e.target.value)} onKeyDown={e => { if (e.key === 'Enter') handleSave(); }}
              className="w-full bg-slate-700 border border-slate-600 text-slate-100 text-sm rounded-lg px-3 py-2 focus:outline-none focus:border-blue-500" placeholder="Nombre del nivel" autoFocus />
          </div>
          <div>
            <label className="text-xs text-slate-400 mb-1 block">Orden</label>
            <input type="number" value={orden} onChange={e => setOrden(Number(e.target.value))} onKeyDown={e => { if (e.key === 'Enter') handleSave(); }}
              className="w-full bg-slate-700 border border-slate-600 text-slate-100 text-sm rounded-lg px-3 py-2 focus:outline-none focus:border-blue-500" placeholder="0" />
          </div>
          <div className="flex gap-3">
            <button onClick={handleSave} disabled={saving} className="flex-1 py-2 bg-blue-600 hover:bg-blue-500 text-white text-sm font-semibold rounded-lg">{saving ? 'Guardando...' : 'Guardar'}</button>
            <button onClick={() => setFormOpen(false)} className="flex-1 py-2 bg-slate-700 hover:bg-slate-600 text-slate-200 text-sm rounded-lg">Cancelar</button>
          </div>
        </div>
      </Modal>

      <Modal open={!!deleteItem} onClose={() => setDeleteItem(null)} title="Confirmar Eliminación" size="sm">
        <div className="space-y-4">
          <p className="text-slate-300 text-sm">¿Eliminar <strong className="text-slate-100">{deleteItem?.nombre}</strong>?</p>
          <div className="flex gap-3">
            <button onClick={handleDelete} className="flex-1 py-2 bg-red-600 hover:bg-red-500 text-white text-sm font-semibold rounded-lg">Eliminar</button>
            <button onClick={() => setDeleteItem(null)} className="flex-1 py-2 bg-slate-700 hover:bg-slate-600 text-slate-200 text-sm rounded-lg">Cancelar</button>
          </div>
        </div>
      </Modal>
    </div>
  );
}
```

---

### 3.7. Menú Sidebar y Registro del Enrutador

Finalmente, para que la página sea navegable, se actualizó la barra lateral y la estructura de enrutamiento principal.

#### En `src/components/layout/Sidebar.tsx`:
Se importó el icono `Award` de la biblioteca `lucide-react` y se añadió una nueva opción bajo el bloque "Configuraciones":
```typescript
// 1. Tipo de página enrutada
export type Page =
  | 'postulaciones'
  | 'analisis'
  | 'tecnologias'
  | 'metodos'
  | 'empresas'
  | 'cargos'
  | 'plataformas'
  | 'modalidades'
  | 'ubicaciones'
  | 'estados'
  | 'niveles'; // Nuevo

// 2. Elemento de navegación registrado
const NAV = [
  { section: 'Principal', items: [
    { id: 'postulaciones' as Page, label: 'Postulaciones', icon: Briefcase },
    { id: 'analisis' as Page, label: 'Analisis', icon: BarChart2 },
  ]},
  { section: 'Configuraciones', items: [
    { id: 'tecnologias' as Page, label: 'Tecnologías', icon: Cpu },
    { id: 'metodos' as Page, label: 'Metodos Evaluaciones', icon: CheckSquare },
    { id: 'empresas' as Page, label: 'Empresas', icon: Building2 },
    { id: 'cargos' as Page, label: 'Cargos', icon: Users },
    { id: 'plataformas' as Page, label: 'Plataformas', icon: Layers },
    { id: 'modalidades' as Page, label: 'Modalidad', icon: Monitor },
    { id: 'ubicaciones' as Page, label: 'Ubicaciones', icon: MapPin },
    { id: 'estados' as Page, label: 'Estados', icon: Tag },
    { id: 'niveles' as Page, label: 'Niveles', icon: Award }, // Nuevo
  ]},
];
```

#### En `src/App.tsx`:
Se importó y renderizó de forma condicional el nuevo componente `NivelesPage`:
```typescript
import NivelesPage from './pages/NivelesPage'; // Importación

// Renderizado condicional
<div className="flex-1 flex flex-col overflow-hidden">
  {page === 'postulaciones' && <PostulacionesPage {...props} />}
  {page === 'analisis' && <AnalisisPage {...props} />}
  {page === 'tecnologias' && <TecnologiasPage {...props} />}
  {page === 'metodos' && <MetodosPage {...props} />}
  {page === 'empresas' && <EmpresasPage {...props} />}
  {page === 'cargos' && <CargosPage {...props} />}
  {page === 'plataformas' && <PlataformasPage {...props} />}
  {page === 'modalidades' && <ModalidadesPage {...props} />}
  {page === 'ubicaciones' && <UbicacionesPage {...props} />}
  {page === 'estados' && <EstadosPage {...props} />}
  {page === 'niveles' && <NivelesPage {...props} />} // Renderizado
</div>
```

---

## 4. Control de Calidad y Pruebas

Para garantizar que el software se encuentra libre de regresiones y errores de sintaxis, se llevaron a cabo pruebas automáticas en la terminal:

### 4.1. Análisis Estático con TypeScript
Se corrió el compilador en modo de solo lectura (Typecheck):
```bash
npm run typecheck
```
**Resultado:** Exitoso. Inicialmente se detectaron dos advertencias de variables declaradas pero no usadas (`colorHex` en `PostulacionForm.tsx` y el import `Filter` en `PostulacionesPage.tsx`), las cuales fueron removidas. La segunda corrida de validación se completó con **cero errores de compilación**.

### 4.2. Compilación de Producción (Production Build)
Para corroborar que el minificador de Vite y Rollup empaquetan correctamente el código para producción, se corrió el bundler:
```bash
npm run build
```
**Resultado:** Exitoso. El proyecto generó las carpetas y archivos estáticos de forma limpia sin alertas de dependencias ni de código no soportado.
```
vite v5.4.8 building for production...
✓ 1495 modules transformed.
dist/index.html                   0.70 kB │ gzip:  0.39 kB
dist/assets/index-CNYGGWPd.css   20.98 kB │ gzip:  4.49 kB
dist/assets/index-U3MAQvJR.js   238.61 kB │ gzip: 62.30 kB
✓ built in 7.04s
```

---

## 5. Guía de Verificación Manual (Paso a Paso)

Para corroborar visualmente el funcionamiento en el entorno local:

1. **Puesta en marcha del proyecto**:
   Ejecuta `npm run dev` en la raíz. Esto levantará concurrentemente el servidor backend (puerto 3001) y el servidor de desarrollo de Vite (puerto 5173).
2. **Revisar el Mantenedor de Niveles**:
   Abre la barra lateral, haz click en **Niveles** e ingresa un nuevo nivel de prueba. Edítalo y posteriormente elimínalo. Valida que el orden numérico se respete.
3. **Guardar una Postulación con los Nuevos Campos**:
   Ingresa a **Postulaciones**, presiona "Agregar Postulación". Llena los nuevos campos (selecciona un nivel de la base de datos, ingresa 200 en Cant. Solicitudes y 1200000 en Sueldo Pedido). Guarda y verifica en la consola que no haya errores de red.
4. **Verificar el Modo Solo Lectura (Ojo)**:
   Pasa el cursor sobre la postulación creada y presiona el icono de **Ojo**. Verifica que el diálogo se abra bajo el título "Detalle de Postulación". Comprueba que todos los dropdowns, inputs de texto y botones de borrado de tecnologías estén completamente inhabilitados y con la opacidad reducida (`disabled`), y que el botón de guardar no se muestre en pantalla.

---

**Fin del reporte.**
